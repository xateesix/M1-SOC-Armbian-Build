# Factory RK3308 images boot with console=ttyFIQ0 (fiq-debugger on UART3).
# Required when using factory boot.img until we ship a custom LZ4 boot.img.
[Service]
ExecStart=
ExecStart=-/sbin/agetty -o '-p -- \\u' --keep-baud 115200,57600,38400,9600 - 1500000 $TERM
